# Copiar a .env en local, o configurar como variable de entorno en Vercel
# (Project Settings → Environment Variables). Nunca se commitea.

# Ticket de integración de Mercado Público (ChileCompra).
# Antes estaba escrito directamente en assets/js/main.js, visible para
# cualquiera que abriera el código fuente. Ahora solo lo usa
# api/licitaciones.js, que corre en el servidor (función serverless).
MP_API_TICKET=
